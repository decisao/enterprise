/*
** DADOS DA AGENDA
*/

CREATE TABLE AGENDA (
  ID INTEIRO_VALIDO,
  PARENTID INTEIRO,
  CODUSUARIO INTEIRO_VALIDO,
  TYPEINT INTEIRO,
  START DATA,
  FINISH DATA,
  OPTIONS INTEIRO,
  CAPTION STR254,
  RECURRENCEINDEX INTEIRO,
  RECURRENCEINFO BLOB,
  RESOURCEID INTEIRO,
  LOCATION STR254,
  MESSAGESTR STR254,
  REMINDER INTEIRO,
  STATE INTEIRO,
  LABELCOLOR INTEIRO,
  CONSTRAINT AGENDA_PK
  PRIMARY KEY (ID));

GRANT ALL ON AGENDA TO PUBLIC;

INSERT INTO SEQUENCIA VALUES (60, 'AGENDA', 1);

ALTER TABLE AGENDA
ADD CONSTRAINT AGENDA_FK01
FOREIGN KEY (CODUSUARIO)
REFERENCES CLIENTES(CODIGO)
ON DELETE CASCADE
ON UPDATE CASCADE;

INSERT INTO SCRIPTVER(NUMERO, DATA) VALUES (339, CURRENT_TIMESTAMP);
COMMIT;

