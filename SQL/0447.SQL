/*
** GED
*/

CREATE TABLE OSGED (
    CODMOVIMENTO INTEIRO_VALIDO NOT NULL,
    NOME STR60_VALIDO NOT NULL,
    EXTENSAO STR03,
    TAMANHO DINHEIRO3,
    DESCRICAO STR60,
    DATA DATA_HOJE NOT NULL);

ALTER TABLE OSGED
ADD CONSTRAINT PK_OSGED
PRIMARY KEY (CODMOVIMENTO,NOME,DATA);

ALTER TABLE OSGED
ADD CONSTRAINT FK_OSGED_1
FOREIGN KEY (CODMOVIMENTO)
REFERENCES MOVIMENTOS(CODIGO)
ON DELETE CASCADE;

GRANT ALL ON OSGED TO PUBLIC;

ALTER TABLE OSGED
ADD ARQUIVO BLOB SUB_TYPE 1 SEGMENT SIZE 80;

ALTER TABLE OSGED
ADD ARQUIVO BLOB SUB_TYPE 0 SEGMENT SIZE 1024;

ALTER TABLE OSGED DROP ARQUIVO;

ALTER TABLE OSGED
ADD ARQUIVO BLOB SUB_TYPE 0 SEGMENT SIZE 80;

INSERT INTO SCRIPTVER(NUMERO, DATA) VALUES (447, CURRENT_TIMESTAMP);
COMMIT;
